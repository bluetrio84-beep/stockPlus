<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockPlus.mapper.AdminMapper">

    <select id="getAllStockCodes" resultType="string">
        SELECT stock_code FROM stock_master
    </select>

    <!-- 1. 수집기 설정 및 로그 -->
    <select id="getCollectorConfig" resultType="map">
        SELECT id, collect_interval, ai_strategy_mode, collect_on_weekend, collect_on_holiday 
        FROM collector_config WHERE id = 1
    </select>

    <update id="updateCollectInterval">
        UPDATE collector_config SET collect_interval = #{interval} WHERE id = 1
    </update>

    <update id="updateAiStrategy">
        UPDATE collector_config SET ai_strategy_mode = #{mode} WHERE id = 1
    </update>

    <update id="updateCollectorPolicy">
        UPDATE collector_config SET collect_on_weekend = #{weekend}, collect_on_holiday = #{holiday} WHERE id = 1
    </update>

    <select id="getLatestIndices" resultType="map">
        SELECT index_name, index_value, change_val, change_rate 
        FROM market_index_history 
        WHERE id IN (SELECT MAX(id) FROM market_index_history GROUP BY index_name)
    </select>

    <select id="getDailyReport" resultType="string">
        SELECT content FROM ai_daily_report WHERE report_date = #{date}
    </select>

    <insert id="insertDailyReport">
        INSERT INTO ai_daily_report (report_date, content) VALUES (#{date}, #{content})
        ON DUPLICATE KEY UPDATE content = #{content}
    </insert>

    <select id="getCollectorLogs" resultType="map">
        SELECT * FROM collector_logs ORDER BY id DESC LIMIT 100
    </select>

    <select id="getHourlyStats" resultType="map">
        SELECT stat_hour as hour, row_count as count, avg_latency_ms as latency 
        FROM collector_hourly_stats 
        ORDER BY stat_hour DESC LIMIT 24
    </select>

    <select id="getCollectedData" resultType="map">
        SELECT * FROM collector_logs WHERE log_level = 'INFO' ORDER BY id DESC LIMIT 20
    </select>

    <select id="getRecentRankings" resultType="map">
        SELECT * FROM stock_rankings ORDER BY captured_at DESC LIMIT 20
    </select>

    <select id="getRecentThemes" resultType="map">
        SELECT * FROM market_themes ORDER BY updated_at DESC LIMIT 20
    </select>

    <select id="getRecentIndustries" resultType="map">
        SELECT * FROM industry_quotes ORDER BY updated_at DESC LIMIT 20
    </select>

    <!-- 3. 인텔리전스 및 대시보드 -->
    <select id="getIndustryHeatmap" resultType="map">
        SELECT i.*, 
        COALESCE((SELECT signal_type FROM ai_prediction WHERE target_name = i.industry_name ORDER BY id DESC LIMIT 1), 'WAIT') as ai_signal,
        COALESCE((SELECT prediction_score FROM ai_prediction WHERE target_name = i.industry_name ORDER BY id DESC LIMIT 1), 50) as ai_score,
        COALESCE(
            (SELECT prediction_score FROM ai_prediction WHERE target_name = i.industry_name ORDER BY id DESC LIMIT 1) - 
            (SELECT prediction_score FROM ai_prediction WHERE target_name = i.industry_name AND created_at &lt; CURDATE() ORDER BY id DESC LIMIT 1), 
            0
        ) as score_diff
        FROM industry_quotes i 
        ORDER BY i.change_rate DESC
    </select>

    <select id="getThemePersistence" resultType="map">
        SELECT theme_name, 
        COALESCE(SUM(avg_change_rate), 0) as total_score, 
        GROUP_CONCAT(DISTINCT lead_stocks SEPARATOR ', ') as lead_stocks 
        FROM market_themes 
        WHERE updated_at &gt;= DATE_SUB(NOW(), INTERVAL 3 DAY) 
        GROUP BY theme_name 
        ORDER BY total_score DESC
    </select>

    <select id="getMarketLeaders" resultType="map">
        SELECT sm.stock_name, sd.foreign_net_buy, sd.top_brokers 
        FROM stock_supply_demand sd JOIN stock_master sm ON sd.stock_code = sm.stock_code 
        WHERE sd.captured_at >= DATE_SUB(NOW(), INTERVAL 24 HOUR) 
        ORDER BY sd.foreign_net_buy DESC LIMIT 10
    </select>

    <select id="getLatestAiSignals" resultType="map">
        SELECT ap.*, sm.stock_name 
        FROM ai_prediction ap
        JOIN stock_master sm ON ap.target_name COLLATE utf8mb4_unicode_ci = CONCAT('STOCK_', sm.stock_code) COLLATE utf8mb4_unicode_ci
        WHERE ap.id &gt;= (SELECT COALESCE(MAX(id), 0) - 3000 FROM ai_prediction)
        AND ap.signal_type NOT IN ('WAIT', 'NORMAL')
        ORDER BY ap.id DESC LIMIT 10
    </select>

    <select id="getMarketBreadth" resultType="map">
        SELECT COUNT(CASE WHEN change_rate &gt; 0 THEN 1 END) as rising_count, 
        COUNT(CASE WHEN change_rate &lt; 0 THEN 1 END) as falling_count, 
        COUNT(CASE WHEN change_rate = 0 THEN 1 END) as steady_count 
        FROM industry_quotes
    </select>

    <select id="getLatestStockSupplyDemand" resultType="map">
        SELECT * FROM stock_supply_demand WHERE stock_code = #{stockCode} ORDER BY id DESC LIMIT 1
    </select>

    <select id="getTopRankings" resultType="map">
        (SELECT 'AMOUNT' as type, stock_name, stock_code FROM stock_rankings WHERE ranking_type = 'AMOUNT' AND captured_at = (SELECT MAX(captured_at) FROM stock_rankings WHERE ranking_type = 'AMOUNT') ORDER BY rank_val ASC LIMIT 3) 
        UNION ALL 
        (SELECT 'RISE' as type, stock_name, stock_code FROM stock_rankings WHERE ranking_type = 'RISE' AND captured_at = (SELECT MAX(captured_at) FROM stock_rankings WHERE ranking_type = 'RISE') ORDER BY rank_val ASC LIMIT 3)
    </select>

    <select id="getAiHitRate" resultType="double">
        SELECT 
        COALESCE(ROUND((COUNT(CASE WHEN i.change_rate &gt; 0 THEN 1 END) / NULLIF(COUNT(*), 0)) * 100, 1), 0) as hit_rate 
        FROM ai_prediction ap 
        JOIN industry_quotes i ON ap.target_name = i.industry_name 
        WHERE ap.id &gt;= (SELECT COALESCE(MAX(id), 0) - 100000 FROM ai_prediction)
        AND ap.signal_type = 'BUY' 
        AND ap.created_at &gt;= DATE_SUB(NOW(), INTERVAL 7 DAY)
    </select>

    <select id="getLeadStocksByIndustryName" resultType="string">
        SELECT lead_stocks FROM industry_quotes WHERE industry_name = #{industryName} ORDER BY updated_at DESC LIMIT 1
    </select>

    <select id="getStocksByNames" resultType="map">
        SELECT m.stock_name as name, m.stock_code as code, 
        0.0 as x, 
        COALESCE((SELECT prediction_score FROM ai_prediction WHERE target_name = CONCAT('STOCK_', m.stock_code) ORDER BY id DESC LIMIT 1), 50) as y,
        COALESCE((SELECT volume FROM stock_supply_demand WHERE stock_code = m.stock_code ORDER BY id DESC LIMIT 1), 0) as volume,
        COALESCE((SELECT foreign_net_buy * current_price FROM stock_supply_demand WHERE stock_code = m.stock_code ORDER BY id DESC LIMIT 1), 0) as z,
        COALESCE((SELECT signal_type FROM ai_prediction WHERE target_name = CONCAT('STOCK_', m.stock_code) ORDER BY id DESC LIMIT 1), 'WAIT') as ai_signal
        FROM stock_master m 
        WHERE 
        <foreach item="item" collection="names" separator=" OR ">
          m.stock_name LIKE CONCAT('%', #{item}, '%') 
        </foreach>
        ORDER BY m.market_cap DESC
    </select>

    <select id="getNextLeadersByDate" resultType="map">
        SELECT * FROM ai_next_leaders 
        WHERE DATE(captured_at) = #{date} 
        ORDER BY total_score DESC, captured_at DESC, stock_code ASC 
        LIMIT 10
    </select>

    <update id="updateNextLeaderFeedback">
        UPDATE ai_next_leaders SET feedback_tag = #{feedbackTag} 
        WHERE stock_code = #{stockCode} AND DATE(captured_at) = #{date}
    </update>

    <insert id="insertFinancials">
        INSERT INTO company_financials (stock_code, report_year, report_code, revenue, op_profit, net_income, roe, created_at) 
        VALUES (#{stock_code}, #{report_year}, #{report_code}, #{revenue}, #{op_profit}, #{net_income}, #{roe}, NOW()) 
        ON DUPLICATE KEY UPDATE revenue = #{revenue}, op_profit = #{op_profit}, net_income = #{net_income}, roe = #{roe}, created_at = NOW()
    </insert>

    <select id="getAiModelPerformance" resultType="map">
        SELECT 'LSTM' as model_name,
        COALESCE(ROUND(COUNT(CASE WHEN lstm_score >= 50 AND hit_result = 'SUCCESS' THEN 1 END) / NULLIF(COUNT(CASE WHEN lstm_score >= 50 AND hit_result != 'PENDING' THEN 1 END), 0) * 100, 1), 0) as hit_rate,
        (SELECT ROUND(weight_lstm * 100) FROM collector_config WHERE id = 1) as weight,
        (SELECT tuning_reason FROM collector_config WHERE id = 1) as tuning_reason
        FROM ai_next_leaders
        UNION ALL
        SELECT 'TCN' as model_name,
        COALESCE(ROUND(COUNT(CASE WHEN tcn_score >= 50 AND hit_result = 'SUCCESS' THEN 1 END) / NULLIF(COUNT(CASE WHEN tcn_score >= 50 AND hit_result != 'PENDING' THEN 1 END), 0) * 100, 1), 0) as hit_rate,
        (SELECT ROUND(weight_tcn * 100) FROM collector_config WHERE id = 1) as weight,
        (SELECT tuning_reason FROM collector_config WHERE id = 1) as tuning_reason
        FROM ai_next_leaders
        UNION ALL
        SELECT 'XGB' as model_name,
        COALESCE(ROUND(COUNT(CASE WHEN xgb_score >= 50 AND hit_result = 'SUCCESS' THEN 1 END) / NULLIF(COUNT(CASE WHEN xgb_score >= 50 AND hit_result != 'PENDING' THEN 1 END), 0) * 100, 1), 0) as hit_rate,
        (SELECT ROUND(weight_xgb * 100) FROM collector_config WHERE id = 1) as weight,
        (SELECT tuning_reason FROM collector_config WHERE id = 1) as tuning_reason
        FROM ai_next_leaders
    </select>
    <select id="getPastRecommendations" resultType="map">
        SELECT stock_code, stock_name, total_score, price_at_recom, price_after_3d, hit_result, DATE_FORMAT(captured_at, '%m.%d') as date
        FROM ai_next_leaders
        WHERE hit_result != 'PENDING' AND is_top10 = 'Y'
        ORDER BY captured_at DESC, total_score DESC LIMIT 10
    </select>
    <select id="getHolidaysByYear" resultType="map">
        SELECT * FROM market_holidays WHERE holiday_year = #{year} ORDER BY holiday_date ASC
    </select>

    <insert id="insertHoliday">
        INSERT INTO market_holidays (holiday_date, holiday_name, holiday_year) VALUES (#{holiday_date}, #{holiday_name}, #{holiday_year})
    </insert>

    <update id="updateHoliday">
        UPDATE market_holidays SET holiday_date = #{holiday_date}, holiday_name = #{holiday_name}, holiday_year = #{holiday_year} WHERE id = #{id}
    </update>

    <delete id="deleteHoliday">
        DELETE FROM market_holidays WHERE id = #{id}
    </delete>

    <select id="checkIsHoliday" resultType="int">
        SELECT COUNT(*) FROM market_holidays WHERE holiday_date = #{date}
    </select>

    <select id="getSmartMoneyStocks" resultType="map">
        SELECT 
            t1.stock_code, 
            t1.stock_name, 
            t1.smart_money_score as max_score, 
            t1.captured_at as last_detected,
            t1.reason,
            t1.price_at_recom
        FROM ai_next_leaders t1
        INNER JOIN (
            SELECT stock_code, MAX(id) as max_id
            FROM ai_next_leaders
            WHERE smart_money_score >= 90 
            AND captured_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
            GROUP BY stock_code
        ) t2 ON t1.id = t2.max_id
        ORDER BY t1.captured_at DESC
    </select>

    <!-- [v16.53] 세력 잠행 매집 추적 (최근 7거래일 기준 외인/기관 순매수 집중 및 평단가 역산) -->
    <select id="getStealthAccumulationStocks" resultType="map">
        SELECT 
            d.stock_code,
            m.stock_name,
            COUNT(d.bsop_date) as days,
            SUM(d.foreign_net_buy) as foreign_buy,
            SUM(d.institution_net_buy) as inst_buy,
            (SUM(d.foreign_net_buy) + SUM(d.institution_net_buy)) as total_smart_buy,
            ROUND(SUM((d.foreign_net_buy + d.institution_net_buy) * d.close_price) / NULLIF(SUM(d.foreign_net_buy + d.institution_net_buy), 0)) as est_avg_price,
            MAX(CASE WHEN d.bsop_date = (SELECT MAX(bsop_date) FROM daily_stock_investor) THEN d.close_price END) as current_price,
            ROUND((MAX(CASE WHEN d.bsop_date = (SELECT MAX(bsop_date) FROM daily_stock_investor) THEN d.close_price END) - 
                   (SUM((d.foreign_net_buy + d.institution_net_buy) * d.close_price) / NULLIF(SUM(d.foreign_net_buy + d.institution_net_buy), 0))) / 
                   (SUM((d.foreign_net_buy + d.institution_net_buy) * d.close_price) / NULLIF(SUM(d.foreign_net_buy + d.institution_net_buy), 0)) * 100, 2) as diff_rate,
            (CASE 
                WHEN SUM(d.foreign_net_buy) > 0 AND SUM(d.institution_net_buy) > 0 THEN '외인/기관 양매수'
                WHEN SUM(d.foreign_net_buy) > SUM(d.institution_net_buy) THEN '외국인 주도'
                ELSE '기관 주도'
             END) as main_actor,
            COALESCE((
                SELECT ROUND(smart_money_score, 1) 
                FROM ai_next_leaders 
                WHERE stock_code = d.stock_code COLLATE utf8mb4_unicode_ci 
                ORDER BY id DESC LIMIT 1
            ), 75.0) as smart_score
        FROM daily_stock_investor d
        JOIN stock_master m ON d.stock_code COLLATE utf8mb4_unicode_ci = m.stock_code COLLATE utf8mb4_unicode_ci
        WHERE d.bsop_date >= (SELECT MIN(bsop_date) FROM (SELECT DISTINCT bsop_date FROM daily_stock_investor ORDER BY bsop_date DESC LIMIT 7) tmp)
        AND m.stock_name NOT LIKE 'KODEX%' AND m.stock_name NOT LIKE 'TIGER%' AND m.stock_name NOT LIKE 'ACE%'
        GROUP BY d.stock_code, m.stock_name
        HAVING total_smart_buy > 20000 AND est_avg_price > 0
        ORDER BY total_smart_buy DESC
        LIMIT 20
    </select>

    <!-- [v16.54] 시장 전환점 나침반 (Market Turning Point / Peak & Bottom) 종합 지표 -->
    <select id="getMarketCompassMetrics" resultType="map">
        SELECT 
            (SELECT COUNT(CASE WHEN change_rate > 0 THEN 1 END) FROM industry_quotes) as rising_industries,
            (SELECT COUNT(CASE WHEN change_rate &lt; 0 THEN 1 END) FROM industry_quotes) as falling_industries,
            (SELECT ROUND(AVG(change_rate), 2) FROM industry_quotes) as avg_industry_rate,
            (SELECT ROUND(AVG(COALESCE(prediction_score, 50)), 1) FROM ai_prediction WHERE id >= (SELECT COALESCE(MAX(id), 0) - 1000 FROM ai_prediction)) as avg_market_ai_score,
            COALESCE(t.rsi_oversold, 0) as rsi_oversold_cnt,
            COALESCE(t.rsi_overbought, 0) as rsi_overbought_cnt,
            COALESCE(t.avg_rsi, 50.0) as avg_rsi,
            COALESCE(t.golden_cross_cnt, 0) as golden_cross_cnt,
            COALESCE(t.total_program_net_buy, 0) as total_program_net_buy,
            COALESCE(t.total_stocks, 0) as total_tracked_stocks
        FROM (
            SELECT 
                COUNT(*) as total_stocks,
                COUNT(CASE WHEN rsi &lt;= 30 THEN 1 END) as rsi_oversold,
                COUNT(CASE WHEN rsi >= 70 THEN 1 END) as rsi_overbought,
                ROUND(AVG(rsi), 1) as avg_rsi,
                COUNT(CASE WHEN is_golden_cross = 1 THEN 1 END) as golden_cross_cnt,
                SUM(program_net_buy) as total_program_net_buy
            FROM stock_intraday_history
            WHERE captured_at >= (SELECT MAX(captured_at) FROM stock_intraday_history) - INTERVAL 20 MINUTE
        ) t
    </select>

    <select id="getMarketCompassInvestorFlow" resultType="map">
        SELECT 
            d.bsop_date as date,
            SUM(d.foreign_net_buy) as foreign_net_buy,
            SUM(d.institution_net_buy) as inst_net_buy,
            SUM(d.individual_net_buy) as indiv_net_buy
        FROM daily_stock_investor d
        WHERE d.bsop_date >= (SELECT MIN(bsop_date) FROM (SELECT DISTINCT bsop_date FROM daily_stock_investor ORDER BY bsop_date DESC LIMIT 7) tmp)
        GROUP BY d.bsop_date
        ORDER BY d.bsop_date ASC
    </select>

    <!-- [v16.50] AI 백테스트 전체 및 최근 30일 성적표 요약 -->
    <select id="getAiPerformanceSummary" resultType="map">
        SELECT 
            COUNT(*) as total_evaluated,
            COUNT(CASE WHEN hit_result = 'SUCCESS' THEN 1 END) as success_cnt,
            ROUND(COUNT(CASE WHEN hit_result = 'SUCCESS' THEN 1 END) / NULLIF(COUNT(*), 0) * 100, 1) as overall_hit_rate,
            ROUND(AVG(CASE WHEN hit_result != 'PENDING' AND price_at_recom > 0 THEN (price_after_3d - price_at_recom) / price_at_recom * 100 END), 2) as avg_return_rate,
            COALESCE((
                SELECT ROUND(COUNT(CASE WHEN hit_result = 'SUCCESS' THEN 1 END) / NULLIF(COUNT(*), 0) * 100, 1)
                FROM ai_next_leaders
                WHERE hit_result IN ('SUCCESS', 'FAIL') AND captured_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
            ), 0.0) as recent_30d_hit_rate,
            COALESCE((
                SELECT stock_name FROM ai_next_leaders 
                WHERE hit_result = 'SUCCESS' AND captured_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
                ORDER BY (price_after_3d - price_at_recom) / price_at_recom DESC LIMIT 1
            ), '-') as best_pick_name,
            COALESCE((
                SELECT ROUND(MAX((price_after_3d - price_at_recom) / price_at_recom * 100), 1)
                FROM ai_next_leaders 
                WHERE hit_result = 'SUCCESS' AND captured_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
            ), 0.0) as best_pick_return
        FROM ai_next_leaders 
        WHERE hit_result IN ('SUCCESS', 'FAIL')
    </select>

    <!-- [v16.50] 장중 실시간 급상승 Next Leaders Top 10 (30분 수급 스냅샷 기반) -->
    <select id="getLiveNextLeaders" resultType="map">
        SELECT 
            h.stock_code,
            m.stock_name,
            h.price,
            h.volume,
            ROUND(h.rsi, 1) as rsi,
            h.program_net_buy,
            h.obv,
            (
                (CASE WHEN h.rsi &lt;= 40 THEN 30 WHEN h.rsi &lt;= 55 THEN 20 ELSE 10 END) +
                (CASE WHEN h.program_net_buy > 0 THEN LEAST(40, ROUND(h.program_net_buy / 10000)) ELSE 0 END) +
                (CASE WHEN h.obv > 0 THEN 20 ELSE 5 END)
            ) as live_score,
            DATE_FORMAT(h.captured_at, '%H:%i') as captured_time
        FROM (
            SELECT * FROM stock_intraday_history
            WHERE captured_at >= (SELECT MAX(captured_at) FROM stock_intraday_history) - INTERVAL 15 MINUTE
        ) h
        JOIN stock_master m ON h.stock_code COLLATE utf8mb4_unicode_ci = m.stock_code COLLATE utf8mb4_unicode_ci
        GROUP BY h.stock_code, m.stock_name, h.price, h.volume, h.rsi, h.program_net_buy, h.obv, h.captured_at
        ORDER BY live_score DESC, h.program_net_buy DESC
        LIMIT 10
    </select>

</mapper>
