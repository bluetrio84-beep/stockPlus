<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockPlus.mapper.UserMarketInsightMapper">

    <select id="findLatestByType" resultType="string">
        SELECT insight_text FROM user_market_insight 
        WHERE USRID = #{usrId} AND insight_type = #{type}
        ORDER BY created_at DESC LIMIT 1
    </select>

    <insert id="insert">
        INSERT INTO user_market_insight (USRID, insight_type, insight_text, created_at)
        VALUES (#{usrId}, #{type}, #{insightText}, NOW())
    </insert>

</mapper>
