<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockPlus.mapper.UserMapper">

    <select id="findAll" resultType="com.stockPlus.domain.User">
        SELECT 
            USRID as usrId, 
            USRNAME as usrName, 
            email, 
            phone_number as phoneNumber, 
            role,
            useyn,
            created_at as createdAt 
        FROM users
    </select>

    <select id="findByUsrId" resultType="com.stockPlus.domain.User">
        SELECT USRID as usrId, USRNAME as usrName, password, phone_number as phoneNumber, email, role, useyn, created_at as createdAt 
        FROM users WHERE USRID = #{usrId}
    </select>

    <insert id="insert" parameterType="com.stockPlus.domain.User">
        INSERT INTO users (USRID, USRNAME, password, phone_number, email, role, useyn, created_at)
        VALUES (#{usrId}, #{usrName}, #{password}, #{phoneNumber}, #{email}, #{role}, COALESCE(#{useyn}, 'Y'), NOW())
    </insert>

    <select id="existsByUsrId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE USRID = #{usrId}
    </select>

    <select id="existsByPhoneNumber" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE phone_number = #{phoneNumber}
    </select>

    <select id="findAllUserIds" resultType="string">
        SELECT USRID FROM users
    </select>

    <update id="updatePassword">
        UPDATE users SET password = #{password} WHERE USRID = #{usrId}
    </update>

    <update id="update" parameterType="com.stockPlus.domain.User">
        UPDATE users 
        SET 
            USRNAME = #{usrName},
            email = #{email},
            phone_number = #{phoneNumber},
            role = #{role},
            useyn = #{useyn}
            <if test="password != null">
                , password = #{password}
            </if>
        WHERE USRID = #{usrId}
    </update>

</mapper>